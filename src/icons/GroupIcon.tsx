import { FunctionComponent } from "react";
import { Icon, IconProps } from "./Icon";

export const GroupIcon: FunctionComponent<IconProps> = (props) => (
  <Icon identifier="lar la-object-group" {...props} />
);
