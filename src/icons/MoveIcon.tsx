import { FunctionComponent } from "react";
import { Icon, IconProps } from "./Icon";

export const MoveIcon: FunctionComponent<IconProps> = (props) => (
  <Icon identifier="lar la-hand-paper" {...props} />
);
