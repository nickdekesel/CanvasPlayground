import { FunctionComponent } from "react";
import { Icon, IconProps } from "./Icon";

export const CursorIcon: FunctionComponent<IconProps> = (props) => (
  <Icon identifier="las la-mouse-pointer" {...props} />
);
