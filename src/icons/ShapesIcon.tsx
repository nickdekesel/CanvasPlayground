import { FunctionComponent } from "react";
import { Icon, IconProps } from "./Icon";

export const ShapesIcon: FunctionComponent<IconProps> = (props) => (
  <Icon identifier="las la-shapes" {...props} />
);
