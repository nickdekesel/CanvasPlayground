import { FunctionComponent } from "react";
import { Icon, IconProps } from "./Icon";

export const PlusIcon: FunctionComponent<IconProps> = (props) => (
  <Icon identifier="las la-plus" {...props} />
);
