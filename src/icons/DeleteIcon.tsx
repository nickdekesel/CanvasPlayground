import { FunctionComponent } from "react";
import { Icon, IconProps } from "./Icon";

export const DeleteIcon: FunctionComponent<IconProps> = (props) => (
  <Icon identifier="lar la-trash-alt" {...props} />
);
