import { FunctionComponent } from "react";
import { Icon, IconProps } from "./Icon";

export const MinusIcon: FunctionComponent<IconProps> = (props) => (
  <Icon identifier="las la-minus" {...props} />
);
