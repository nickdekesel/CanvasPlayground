import { FunctionComponent } from "react";
import { Icon, IconProps } from "./Icon";

export const CameraIcon: FunctionComponent<IconProps> = (props) => (
  <Icon identifier="las la-video" {...props} />
);
