import { FunctionComponent } from "react";
import { Icon, IconProps } from "./Icon";

export const LightBulbIcon: FunctionComponent<IconProps> = (props) => (
  <Icon identifier="lar la-lightbulb" {...props} />
);
